'use client';

import Link from 'next/link';
import Card from '@/components/Card';
import { useAuth } from '@/context/AuthContext';
import { ping } from '@/services/api';
import { useState, useEffect } from 'react';

export default function Home() {
  const { user } = useAuth();
  const [apiStatus, setApiStatus] = useState<string | null>(null);
  const [isLoading, setIsLoading] = useState(false);

  useEffect(() => {
    async function checkApi() {
      setIsLoading(true);
      try {
        const result = await ping();
        setApiStatus('API conectada: ' + result);
      } catch (error) {
        setApiStatus('Error al conectar con la API');
        console.error('Error al conectar con la API:', error);
      } finally {
        setIsLoading(false);
      }
    }

    checkApi();
  }, []);

  return (
    <div className="space-y-8">
      {/* Cabecera con título y descripción */}
      <div className="text-center mb-12">
        <h1 className="text-4xl font-extrabold tracking-tight text-gray-900 sm:text-5xl md:text-6xl">
          <span className="block">Plataforma de Trazabilidad</span>
          <span className="block text-indigo-600">Hyperledger Fabric</span>
        </h1>
        <p className="mt-4 max-w-2xl mx-auto text-xl text-gray-500">
          Seguimiento completo de productos desde su origen hasta el consumidor final con tecnología blockchain.
        </p>
      </div>

      {/* Estado de la conexión API */}
      <div className="text-center mb-6">
        <span className={`inline-flex items-center px-3 py-1 rounded-full text-sm font-medium ${
          isLoading ? 'bg-yellow-100 text-yellow-800' : 
          apiStatus && apiStatus.includes('Error') ? 'bg-red-100 text-red-800' : 'bg-green-100 text-green-800'
        }`}>
          {isLoading ? (
            <>
              <svg className="animate-spin -ml-1 mr-2 h-4 w-4 text-yellow-800" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                <circle className="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" strokeWidth="4"></circle>
                <path className="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
              </svg>
              Comprobando conexión...
            </>
          ) : apiStatus}
        </span>
      </div>

      {/* Tarjetas de características */}
      <div className="grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3">
        <Card title="Trazabilidad completa" className="h-full">
          <p>Seguimiento del ciclo de vida completo del producto, desde la materia prima hasta el consumidor final.</p>
          <div className="mt-4">
            <Link href="/trazabilidad" className="text-indigo-600 hover:text-indigo-800 font-medium">
              Consultar trazabilidad →
            </Link>
          </div>
        </Card>
        
        <Card title="Verificación de autenticidad" className="h-full">
          <p>Verificación de la autenticidad y procedencia de los productos mediante registros inmutables en blockchain.</p>
          {!user && (
            <div className="mt-4">
              <Link href="/login" className="text-indigo-600 hover:text-indigo-800 font-medium">
                Iniciar sesión →
              </Link>
            </div>
          )}
        </Card>
        
        <Card title="Certificaciones y datos IoT" className="h-full">
          <p>Integración de certificados de calidad, denominaciones de origen y datos en tiempo real mediante sensores IoT.</p>
        </Card>
      </div>

      {/* Sección para usuarios autenticados */}
      {user && (
        <div className="mt-10">
          <h2 className="text-2xl font-bold text-gray-900 mb-6">Acciones disponibles para {user.role}</h2>
          <div className="bg-white shadow-md rounded-lg overflow-hidden">
            <div className="px-6 py-4">
              <div className="grid grid-cols-1 gap-4 sm:grid-cols-2">
                {user.role === 'viticultor' && (
                  <>
                    <Link href="/cosechas" className="block p-4 border border-gray-200 rounded-lg hover:bg-indigo-50">
                      📋 Gestionar mis cosechas
                    </Link>
                    <Link href="/cosechas/nueva" className="block p-4 border border-gray-200 rounded-lg hover:bg-indigo-50">
                      ✨ Registrar nueva cosecha
                    </Link>
                  </>
                )}
                
                {user.role === 'bodega' && (
                  <>
                    <Link href="/productos" className="block p-4 border border-gray-200 rounded-lg hover:bg-indigo-50">
                      🍷 Gestionar productos
                    </Link>
                    <Link href="/elaboracion" className="block p-4 border border-gray-200 rounded-lg hover:bg-indigo-50">
                      🏭 Proceso de elaboración
                    </Link>
                  </>
                )}
                
                {user.role === 'transportista' && (
                  <>
                    <Link href="/transportes" className="block p-4 border border-gray-200 rounded-lg hover:bg-indigo-50">
                      🚚 Mis transportes
                    </Link>
                    <Link href="/incidencias" className="block p-4 border border-gray-200 rounded-lg hover:bg-indigo-50">
                      ⚠️ Registro de incidencias
                    </Link>
                  </>
                )}
              </div>
            </div>
          </div>
        </div>
      )}
    </div>
  );
}
          </a>
        </div>
      </main>
      <footer className="row-start-3 flex gap-[24px] flex-wrap items-center justify-center">
        <a
          className="flex items-center gap-2 hover:underline hover:underline-offset-4"
          href="https://nextjs.org/learn?utm_source=create-next-app&utm_medium=appdir-template-tw&utm_campaign=create-next-app"
          target="_blank"
          rel="noopener noreferrer"
        >
          <Image
            aria-hidden
            src="/file.svg"
            alt="File icon"
            width={16}
            height={16}
          />
          Learn
        </a>
        <a
          className="flex items-center gap-2 hover:underline hover:underline-offset-4"
          href="https://vercel.com/templates?framework=next.js&utm_source=create-next-app&utm_medium=appdir-template-tw&utm_campaign=create-next-app"
          target="_blank"
          rel="noopener noreferrer"
        >
          <Image
            aria-hidden
            src="/window.svg"
            alt="Window icon"
            width={16}
            height={16}
          />
          Examples
        </a>
        <a
          className="flex items-center gap-2 hover:underline hover:underline-offset-4"
          href="https://nextjs.org?utm_source=create-next-app&utm_medium=appdir-template-tw&utm_campaign=create-next-app"
          target="_blank"
          rel="noopener noreferrer"
        >
          <Image
            aria-hidden
            src="/globe.svg"
            alt="Globe icon"
            width={16}
            height={16}
          />
          Go to nextjs.org →
        </a>
      </footer>
    </div>
  );
}
